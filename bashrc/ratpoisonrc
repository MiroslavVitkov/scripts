# ~/.ratpoisonrc
# Minimal config file for ratpoison.

# Meta key - Windows key.
# Press before command, not simultaneously.

# Basic commands:
# s, S - split screen into "frames"
# <space> - within a frame, cycle the "windows" open inside it
# <tab> - next frame
# c - create a terminal window in the current frame 
# r - resize this frame
# o - only frame - destroy all other frames
# R - remove current frame
# Q - quit ratpoison
#
# The same keys also work in bg_pho-utf8 layout.

escape Super_L

unbind Q
bind Q quit
bind o only

set startupmessage 0

bind Cyrillic_YA quit
bind Cyrillic_o only
bind Cyrillic_ka kill
bind Cyrillic_tse exec xterm
bind Cyrillic_es split
bind Cyrillic_ES hsplit

# TODO: shift + space - previous tab
